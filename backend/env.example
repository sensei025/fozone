# ============================================
# Configuration Backend - Starlink Tickets
# ============================================
# Copiez ce fichier vers .env et remplissez les valeurs
# cp env.example .env

# ============================================
# Configuration du serveur
# ============================================
PORT=3000
NODE_ENV=development

# URL de base de l'API (pour les callbacks)
# Exemple: http://localhost:3000 (dev) ou https://api.votredomaine.com (prod)
API_BASE_URL=http://localhost:3000

# URL de base du frontend (pour les redirections après paiement)
# Exemple: http://localhost:5173 (dev) ou https://votredomaine.com (prod)
FRONTEND_URL=http://localhost:5173

# ============================================
# Configuration Supabase
# ============================================
# Récupérez ces valeurs dans votre projet Supabase:
# Dashboard > Settings > API

# URL de votre projet Supabase
# Format: https://xxxxx.supabase.co
SUPABASE_URL=https://votre-projet.supabase.co

# Service Role Key (clé avec tous les privilèges)
# ⚠️ NE JAMAIS EXPOSER CETTE CLÉ AU FRONTEND
SUPABASE_SERVICE_ROLE_KEY=votre_service_role_key_ici

# Anon Key (clé publique, peut être utilisée côté frontend)
SUPABASE_ANON_KEY=votre_anon_key_ici

# ============================================
# Configuration JWT (Authentification)
# ============================================
# Générez une clé secrète forte (minimum 32 caractères)
# Vous pouvez utiliser: openssl rand -base64 32
JWT_SECRET=votre_jwt_secret_tres_long_et_securise_ici_minimum_32_caracteres

# Durée de validité du token JWT
# Format: 1h, 7d, 30d, etc.
JWT_EXPIRES_IN=7d

# ============================================
# Configuration Moneroo (Paiements Mobile Money)
# ============================================
# Récupérez ces valeurs depuis votre compte Moneroo

# Clé API Moneroo (Secret Key - à utiliser côté backend uniquement)
# ⚠️ IMPORTANT: Utilisez la SECRET KEY, pas la PUBLIC KEY
# La Secret Key commence généralement par "sk_" ou "test_" pour le sandbox
# Récupérez-la depuis: Dashboard Moneroo > Developers > API Keys
MONEROO_API_KEY=votre_moneroo_api_key

# Secret API Moneroo (optionnel, non utilisé actuellement)
MONEROO_API_SECRET=votre_moneroo_api_secret

# Secret pour vérifier les webhooks Moneroo
MONEROO_WEBHOOK_SECRET=votre_moneroo_webhook_secret

# URL de base de l'API Moneroo
# L'API Moneroo utilise https://api.moneroo.io/v1 comme base URL
# Production: https://api.moneroo.io (le /v1 sera ajouté automatiquement)
MONEROO_BASE_URL=https://api.moneroo.io

# ============================================
# Configuration CORS
# ============================================
# URL(s) autorisées pour les requêtes CORS
# Pour le développement local avec React (Vite)
CORS_ORIGIN=http://localhost:5173

# Pour la production, utilisez votre domaine:
# CORS_ORIGIN=https://votredomaine.com

# Pour plusieurs origines, séparez par des virgules:
# CORS_ORIGIN=http://localhost:5173,https://votredomaine.com

# ============================================
# Configuration Logging
# ============================================
# Niveaux: error, warn, info, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# Notes importantes
# ============================================
# 1. Ne commitez JAMAIS le fichier .env dans Git
# 2. Le fichier .env est déjà dans .gitignore
# 3. Pour la production, configurez ces variables sur votre plateforme de déploiement
#    (Vercel, Heroku, Railway, etc.)
# 4. Assurez-vous que JWT_SECRET est unique et sécurisé
# 5. SUPABASE_SERVICE_ROLE_KEY ne doit jamais être exposée au frontend

